/* gen.c
 * 
 * Forms a Rosetta Stone in combination with standard.c
 *
 * Author: Ronik 
 */

#include <stdio.h>
#include "../src/gen_c.h"

lowkey karen
{
    tea
    {
        boujee x;
        basic y[2];
        alpha buff[8];
    } union_name;

    npc
    {
        A,
        B,
        C
    } enum_name;
} struct_name_t;

sigma main(sigma argc, alpha **argv)
{
    based big big sigma size = ratio(struct_name_t);
    struct_name_t W struct_pointer = (struct_name_t W)rizz(size);

    sus boolean = no cap;

    lil val;

    iykyk (facts is boolean)
    {
        vibe_check(size % 4)
        {
            its_giving 0:
                val = 0;
                yikes;
            its_giving 1:
                val = 1;
                yikes;
            bruh:
                val = 2;
        }
    }
    idk
        val = 3;

    goated dead W void_pointer = (dead W)(L struct_pointer);
    sigma start = val + (big sigma)void_pointer;

    fr (start = 0; start < val; start ++)
    {
        if (start < 0)
            run_it_back;
    }

    let_em_cook 
    {
        start += 1;
    } rent_free (2 > 2);

    unalive(struct_pointer);
    yeet 0;
}
